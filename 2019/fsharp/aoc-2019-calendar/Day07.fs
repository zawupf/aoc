module Day07

open System
open System.IO


let job1 prefix =
    raise (NotImplementedException())
    File.ReadLines(prefix + "inputs/Day07.txt") |> string

let job2 prefix =
    raise (NotImplementedException())
    File.ReadLines(prefix + "inputs/Day07.txt") |> string
